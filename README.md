## InvenX (Internal Inventory Management System)

This repo is built **incrementally, module-by-module**.

### Structure
- `backend/`: Node + Express API (MongoDB in later modules)
- `frontend/`: React UI (theme + modules added incrementally)

### Module 1 notes
- This module only scaffolds folders + minimal entry files.
- No database, authentication, inventory, or alerts are implemented yet.

